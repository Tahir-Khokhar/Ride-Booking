# TODO

- [x] Recreate broken virtual environment: delete `myvenv/`, recreate it, and reinstall from `requirements.txt`.
- [x] Run `python manage.py migrate`.
- [x] Run `python manage.py runserver` and confirm it starts.
- [ ] If still hanging: pin Django to a compatible version and re-test.


